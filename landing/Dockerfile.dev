# Development Dockerfile for OpenNoiseNet Landing Page
FROM node:18-alpine

RUN apk add --no-cache libc6-compat curl

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]